const choseColor = product => {
    if (product === "IJ" || product === "Inteliji IDEA") return "#fc315d"
    if (product === "WS" || product === "WebStorm") return "#00ccd6"
    return "#a44af2"
}

export default choseColor;

